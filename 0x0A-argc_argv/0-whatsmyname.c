#include <stdio.h>
/**
 * main-: print it's name
 * @argc: int
 * @argv: array
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	printf("Program name: %s\n", argv[0]);
	return (0);
}
