#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 *
 * @n: integer to make it absolute
 * Return: int or int x -1
 *
 */

int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}
